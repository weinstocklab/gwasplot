# Take a random sample of rows from the summary stats table

Take a random sample of rows from the summary stats table

## Usage

``` r
compute_sample(g, rows = 100L)
```

## Arguments

- g:

  A GWASFormatter object.

- rows:

  The number of rows to sample. Default is 100.
