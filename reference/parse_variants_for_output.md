# Parse variants to extract CHROM, POS, REF, ALT for output

Parse variants to extract CHROM, POS, REF, ALT for output

## Usage

``` r
parse_variants_for_output(variants)
```

## Arguments

- variants:

  Input variants in format "chr_pos_ref_alt"

## Value

data.frame with original variant and parsed components
