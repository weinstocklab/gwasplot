# Format variants for VEP API input

Format variants for VEP API input

## Usage

``` r
format_variants_for_vep(variants)
```

## Arguments

- variants:

  Input variants in format "chr_pos_ref_alt"

## Value

Character vector of VCF-formatted variant strings
